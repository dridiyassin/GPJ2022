using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemStats : MonoBehaviour
{

    //3 types of score bch el mix ynajm yasna3lk haja 7asb formule l Mix

    //Formule Mix: (itemScore1(I1) * itemScore3(I2)) + (itemScore2(I1) * itemScore1(I2)) + (itemScore3(I1) * itemScore2(I2)) mawjoud fel Mixer Script

    public float itemScore1;
    public float itemScore2;
    public float itemScore3;
  
}
